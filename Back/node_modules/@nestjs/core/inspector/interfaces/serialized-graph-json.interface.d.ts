import { SerializedGraphStatus } from '@nestjs/core/inspector/serialized-graph';
import { Edge } from '@nestjs/core/inspector/interfaces/edge.interface';
import { Entrypoint } from '@nestjs/core/inspector/interfaces/entrypoint.interface';
import { Extras } from '@nestjs/core/inspector/interfaces/extras.interface';
import { Node } from '@nestjs/core/inspector/interfaces/node.interface';
import { SerializedGraphMetadata } from '@nestjs/core/inspector/interfaces/serialized-graph-metadata.interface';
export interface SerializedGraphJson {
    nodes: Record<string, Node>;
    edges: Record<string, Edge>;
    entrypoints: Record<string, Entrypoint<unknown>[]>;
    extras: Extras;
    status?: SerializedGraphStatus;
    metadata?: SerializedGraphMetadata;
}
